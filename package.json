{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/stencil-library",
    "packages/react-library"
  ],
  "scripts": {
    "build": "npx lerna run build",
    "test": "npx lerna run test"
  },
  "devDependencies": {
    "@types/node": "^25.0.10",
    "lerna": "^9.0.3",
    "typescript": "^5.0.3"
  },
  "overrides": {
    "esbuild": "^0.27.0"
  }
}
