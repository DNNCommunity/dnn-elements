{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/stencil-library",
    "packages/react-library"
  ],
  "scripts": {
    "build": "npx lerna run build",
    "test": "npx lerna run test"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "lerna": "^9.0.3",
    "typescript": "^5.0.3"
  }
}
